#language: generic

sudo: required
services:
    - docker

before_install:
    - docker build -t transpaclean/productionworkflow -f Dockerfile.dev .

script:
    - docker run -e CI=true transpaclean/productionworkflow npm run test
